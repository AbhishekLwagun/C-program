#include<stdio.h>

    int main(){
        float r, pi=3.142, area;
        printf("enter the radius of a circle:\n");
        scanf("%f", &r);
        area = pi*r*r;
        printf("the area of a circle is %f\n", area);
    return 0;
}1--